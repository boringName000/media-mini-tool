<!-- 顶部banner -->
<view class="top-banner">
  <view class="banner-content">
    <view class="banner-icon">🔒</view>
    <view class="banner-text">本程序仅供签约达人使用，不对外开放（内部使用）</view>
  </view>
</view>

<!-- 喇叭滚动条幅 -->
<view class="notice-banner">
  <view class="notice-content">
    <view class="notice-scroll-container">
      <view class="notice-text {{isScrolling ? 'scrolling' : ''}}">
        <text class="notice-icon">📢</text>
        通知：欢迎各位达人使用本创作工具，请大家坚持日更，按时回传数据，有任何问题及时联系我们！
      </view>
    </view>
  </view>
</view>

<!-- 数据按钮区域 -->
<view class="data-buttons-area">
  <view class="data-button" bind:tap="goTodayData">
    <view class="data-icon">📊</view>
    <view class="data-text">今日数据</view>
  </view>
  
  <view class="data-button" bind:tap="goSettlement">
    <view class="data-icon">💰</view>
    <view class="data-text">结算收益</view>
  </view>
</view>

<scroll-view class="container" scroll-y="true" enhanced="true" show-scrollbar="false">
  <!-- 赛道中心区域 -->
  <view class="track-center-area">
    <view class="track-center-title">赛道中心</view>
    <view class="track-center-content">
      <view 
        class="track-item" 
        wx:for="{{trackTypes}}" 
        wx:key="index"
        data-index="{{index}}"
        catch:tap="onTrackItemTap"
      >
        <view class="track-item-icon">{{item.icon}}</view>
        <view class="track-item-content">
          <view class="track-item-name">{{item.name}}</view>
          <view class="track-item-description">{{item.description}}</view>
        </view>
      </view>
    </view>
  </view>
</scroll-view>