<!-- 智能图片组件 - 自动处理云存储、普通图片和临时文件 -->
<!-- 所有图片都使用统一的image组件 -->
<image 
  class="{{class}}"
  src="{{displayUrl}}" 
  mode="{{mode}}"
  lazy-load="{{lazyLoad}}"
  bindtap="onImageTap"
  bindload="onImageLoad"
  binderror="onImageError"
/>
